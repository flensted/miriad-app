# Cast Frontend Environment Variables
# Copy this file to .env.local for local development

# Backend URL - single source of truth for API and WebSocket connections
# Defaults to http://localhost:3234 if not set
# VITE_BACKEND_URL=http://localhost:3234

# WebSocket URL (optional - only needed for AWS WebSocket API Gateway)
# If not set, WebSocket URL is derived from VITE_BACKEND_URL (http->ws)
# VITE_WS_URL=wss://xxx.execute-api.us-east-1.amazonaws.com/stage

# Auth mode: 'dev' or 'workos'
# - dev: Shows LoginPage with space picker (default for local development)
# - workos: Redirects to /auth/login for WorkOS OAuth flow (production)
# VITE_AUTH_MODE=dev
